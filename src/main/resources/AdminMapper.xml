<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ag.dao.AdminDao">
    <select id="selectAdminByUsername" resultType="Admin">
        select * from admin where username=#{username}
    </select>

    <select id="queryAll" resultType="Admin">
        select * from admin
    </select>

    <select id="queryRoleByAdminId" resultType="Role">
        select r.id,r.role_name as rolename
        FROM role r LEFT JOIN admin_role p on r.id=p.role_id WHERE admin_id=#{adminId}
    </select>

    <select id="queryPermissionByRoleId" resultType="java.lang.String">
        SELECT pe.permission_name
	        FROM role r LEFT JOIN role_permission p ON r.id=p.role_id LEFT JOIN permission pe ON pe.id=p.permission_id where r.id=#{roleId}
    </select>
</mapper>